name: backend-inOrbit

services:
    pg:
        image: bitnami/postgresql:13.16.0
        ports:
            - "5432:5432"
        environment:
            - POSTGRES_USER=docker
            - POSTGRES_PASSWORD=docker123
            - POSTGRES_DB=postgres-inorbit
